# Environment variables for local development with `vercel dev`.
# Production hodnoty se nastavují v Vercel dashboardu (Project Settings -> Environment Variables).

# Pokud je nastaveno na "true", API vrací v chybové odpovědi i Python traceback.
# DOPORUČENO: ponechat false v produkci, true jen pro lokální debug.
DEBUG_MODE=false
